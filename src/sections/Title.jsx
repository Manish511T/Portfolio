import React from 'react';

const Title = () => {
  return (
    <div className="w-screen h-[200px] flex items-center justify-center text-center">
      <h1 className="text-4xl md:text-4xl font-bold bg-gradient-to-r from-blue-900 via-pink-500 to-purple-900 bg-clip-text text-transparent drop-shadow-[0_0_8px_rgba(0,255,255,0.3)]">
        I can think like a system builder, code like a pro, and present like a designer.
      </h1>
    </div>
  );
};

export default Title;
